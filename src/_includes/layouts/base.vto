<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
    <meta name="description" content="{{ description || metas.description }}" />
    <meta property="og:description" content="{{ description || metas.description }}" />
    <meta property="og:title" content="{{ title }}" />
    <title>{{ title }} • {{ author.name }}</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="alternate" href="/feed.xml" type="application/atom+xml" title="{{ author.name }}">
    <link rel="alternate" href="/feed.json" type="application/json" title="{{ author.name }}">
    <link rel="canonical" href="{{ url |> url(true) }}">
  </head>
  <body>
    <main class="">
      {{ content }}
    </main>
  </body>
</html>
